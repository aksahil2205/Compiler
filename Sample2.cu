/*This is a code with incorrect syntax and i don't code like this */

int fucntion_2(int x, char *y){}
	int t = -1;
	while(t >= x){
		t = x;
	{
	return 1;
}

/* You may add other things and it won't parse it */
